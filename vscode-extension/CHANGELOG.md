# Changelog

## 0.1.0

First release. Registers the `WordMcp.McpServer` MCP server with VS Code and launches it through
`dnx`, with settings to pin a server version and to keep sessions in the background service.
